import { styled } from '@mui/material/styles';

export const PageContainer = styled('div')(() => ({
  width: '100%',
  height: '100%',
  display: 'flex',
  overflowY: 'auto',
  overflowX: 'hidden',
}));
